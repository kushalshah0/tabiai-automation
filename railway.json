{
  "$schema": "https://railpack.com",
  "build": {
    "builder": "nixpacks"
  },
  "steps": {
    "install": {
      "commands": [
        "python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install --no-cache-dir setuptools playwright==1.42.0 playwright-stealth==1.0.6"
      ]
    }
  },
  "deploy": {
    "startCommand": "python tabi_checkin.py"
  }
}
